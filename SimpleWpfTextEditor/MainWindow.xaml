<Window x:Class="SimpleWpfTextEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleWpfTextEditor"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="450" Width="800"
        DataContext="{DynamicResource ApplicationDataInstance}">
    <Window.Resources>
        <local:ApplicationData x:Key="ApplicationDataInstance"/>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Open"
                        Executed="OpenFile"/>
        <CommandBinding Command="ApplicationCommands.Save"
                        Executed="SaveFile"
                        CanExecute="AllowSaving"/>
        <CommandBinding Command="ApplicationCommands.SaveAs"
                        Executed="SaveFileAs"/>
    </Window.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Command="ApplicationCommands.SaveAs"
                    Gesture="Ctrl+Shift+S"/>
    </Window.InputBindings>
    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top" Background="#EEE">
            <MenuItem Header="_File">
                <MenuItem Name="MenuItemOpen"
                          Header="_Open"
                          Command="ApplicationCommands.Open"/>
                <MenuItem Name="MenuItemSave"
                          Header="_Save"
                          Command="ApplicationCommands.Save"/>
                <MenuItem Name="MenuItemSaveAs"
                          Header="Save _As"
                          Command="ApplicationCommands.SaveAs"
                          InputGestureText="Ctrl+Shift+S"/>
            </MenuItem>
        </Menu>
        <TextBox Name="MainTextBox"
                 Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 TextChanged="MainTextBox_TextChanged">
        </TextBox>
    </DockPanel>
</Window>